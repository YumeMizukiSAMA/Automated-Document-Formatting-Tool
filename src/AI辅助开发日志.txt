6.7 询问Deepseek修正handleChoice()方法的逻辑错误
   问题
   catch (InputMismatchException e) {
       System.out.println("输入类型不正确！请重试！");
       input.nextInt();  // ❌ 这里错了
   }
    修正
    在 catch 里用 input.next() 或 input.nextLine() 把那个无效数据吃掉，再进入下一轮循环。
    catch (InputMismatchException e) {
        System.out.println("输入类型不正确！请重试！");
        input.next();  // 清掉无效的输入
    }
6.8 询问Deepseek如何检查文件的后缀名
    String类型检测后缀方法String.endsWith("")

    询问DeepseekString path = input.nextLine();是否需要保护
    不需要，其读取的String类型绝对安全

    查询文件输入流相关知识

    对异常对象e如何查看信息
    简短的错误描述：e.getMessage();
    异常的错误类型和简短描述：e.toString();
    打印出最完整的错误信息：i.printStackTrace();

    向Deepseek询问reader.readLine()的工作原理
    一行行读取文件直到末尾返回null

    利用Deepseek修正loadDocument()方法的部分逻辑错误

    询问Deepseek如何将loadDocument()方法与main方法链接
    因为loadDocument()方法不是静态方法，所以应该先创建DocumentFormatter的对象，用对象来调用成员方法

    询问Deepseek文档清理的思路并提供方法
     unifyNewlines();       // 1. 统一换行符
     removeTrailingSpaces();// 2. 去除行尾空格
     mergeBlankLines();     // 3. 合并连续空行
     standardizePunctuation(); // 4. 其他进阶规则...
     用正则表达式和结合String.replaceAll(正则1,正则2)完成批量替换
6.9 如何因为不正确的某些原因结束一个方法
    return即可

    如何检查content内容不为空
    用实例方法String.isEmpty()判断字符串是否为空，返回值为boolean类型

    询问文件写入流的相关知识

    对保存相关代码进行优化更新，增加自动生成建议文件名（通过String.replace("target","replacement")方法替换原有文件名）



